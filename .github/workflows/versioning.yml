name: Bump version
on:
  workflow_run:
    workflows: 
      - gerador imagens docker
      - gerador pacotes nuget
    types:
      - completed
      
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      
      - run: echo ${{ github.event.workflow_run }}
